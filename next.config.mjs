/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí puedes agregar otras configuraciones de Next.js si las necesitas en el futuro
  reactStrictMode: true,
};

export default nextConfig;